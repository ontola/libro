// @flow
import React, { PropTypes } from 'react';
// import { connect } from 'react-redux';
// import { List, ArgumentShow } from '../components';
// import { Argument } from '../models';
//
// const propTypes = {
//   motionId: PropTypes.string.isRequired,
//   side: PropTypes.oneOf(['pro', 'con']).isRequired,
// };
//
// const renderArgument = (argument) => (
//   <ArgumentShow
//     key={argument.id}
//     data={argument}
//   />
// );
//
// const mapStateToProps = (state, ownProps) => {
//   const motion = state.getIn(['motions', 'items', ownProps.motionId]);
//   if (motion && motion.has('arguments')) {
//     console.log(motion.get('arguments'));
//   }
//   return {
//     list: state.getIn(['argumentations', 'items', ownProps.motionId]),
//     renderItem: renderArgument,
//   };
// };
//
// const ArgumentsContainer = connect(
//   mapStateToProps
// )(List);
//
// ArgumentsContainer.propTypes = propTypes;

const ArgumentsContainer = () => <div>Joe</div>;

export default ArgumentsContainer;
