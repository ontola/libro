Simple collapse component. Accessible for screenreaders. 
ses React-Collapse for smooth animation, which in turn uses 
css transitions.

**Use the `CollapsibleContainer` component instead of this one.**
