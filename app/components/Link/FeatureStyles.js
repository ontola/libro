import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  bold: {
    fontWeight: 600,
  },
  centered: {
    alignItems: 'center',
  },
  padded: {
    display: 'block',
    padding: '.3rem 1.3rem',
  },
}));
