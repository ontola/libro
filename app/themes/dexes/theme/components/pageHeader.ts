import { MaterialStyleMap } from '../../../themes';

const pageHeader = (): MaterialStyleMap => ({
  PageHeader: {
    pageHeader: {
      background: 'none',
    },
    pageHeaderInner: {
      backgroundColor: 'none',
    },
  },
});

export default pageHeader;
